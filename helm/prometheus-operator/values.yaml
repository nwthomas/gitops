# Default values for prometheus-operator
# This chart replicates the bundle.yaml exactly

# Enable CRDs (Custom Resource Definitions)
# Set to false if CRDs are already installed in the cluster
crds:
  enabled: true
