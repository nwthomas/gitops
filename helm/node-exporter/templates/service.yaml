apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: node-exporter
  name: node-exporter
  namespace: {{ .Release.Namespace }}
spec:
  clusterIP: None
  ports:
    - name: https
      port: {{ .Values.service.port }}
      targetPort: https
  selector:
    app.kubernetes.io/name: node-exporter
