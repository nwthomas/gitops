{{- if .Values.atlantis.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "atlantis.fullname" . }}
  labels:
    {{- include "atlantis.labels" . | nindent 4 }}
  {{- with .Values.atlantis.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.atlantis.ingress.className }}
  ingressClassName: {{ .Values.atlantis.ingress.className }}
  {{- end }}
  {{- if .Values.atlantis.ingress.tls }}
  tls:
    {{- range .Values.atlantis.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.atlantis.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "atlantis.fullname" $ }}
                port:
                  number: {{ $.Values.atlantis.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
