{{- if .Values.patchJob.create -}}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "vllm.fullname" . }}-lb-ip-patch
  namespace: {{ include "vllm.namespace" . }}
  labels:
    {{- include "vllm.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
spec:
  ttlSecondsAfterFinished: 30
  template:
    metadata:
      name: {{ include "vllm.fullname" . }}-lb-ip-patch
    spec:
      serviceAccountName: {{ include "vllm.serviceAccountName" . }}
      restartPolicy: OnFailure
      containers:
        - name: kubectl
          image: bitnami/kubectl:latest
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -e
              IP=$(kubectl get secret {{ include "vllm.fullname" . }}-lb-ip -n {{ include "vllm.namespace" . }} -o jsonpath="{.data.loadBalancerIP}" | base64 -d)
              CURRENT_IP=$(kubectl get svc {{ include "vllm.fullname" . }} -n {{ include "vllm.namespace" . }} -o jsonpath="{.spec.loadBalancerIP}" 2>/dev/null || echo "")
              if [[ "$CURRENT_IP" != "$IP" ]]; then
                kubectl patch svc {{ include "vllm.fullname" . }} -n {{ include "vllm.namespace" . }} -p '{"spec":{"loadBalancerIP":"'$IP'"}}' --type=merge
              fi
{{- end }}
